export default function AdminLoading() {
  return (
    <div className="flex items-center justify-center min-h-[60vh]">
      <span className="text-brand-yellow text-xs font-semibold tracking-[0.25em] uppercase animate-pulse">
        Cargando...
      </span>
    </div>
  );
}
